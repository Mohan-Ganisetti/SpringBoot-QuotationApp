# Use a base Java image
FROM eclipse-temurin:17-jdk-alpine

# Set working directory
WORKDIR /app

# Copy WAR file
COPY target/QuotationGenerator-0.0.1-SNAPSHOT.war app.war

# Expose port
EXPOSE 8080

# Run WAR with embedded Tomcat
ENTRYPOINT ["java","-jar","app.war"]
